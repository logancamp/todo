// Replace the entire Coordinator block in ScrollHost.swift.
// All conformances declared on the class itself — no extensions on a
// nested class from a generic context (Swift forbids @objc members there).

